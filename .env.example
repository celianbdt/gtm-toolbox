# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-anon-key>
SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>

# Site URL — used as emailRedirectTo base in magic link emails.
# Set to your Vercel production URL in Vercel env vars.
# In local dev, leave unset (falls back to window.location.origin).
NEXT_PUBLIC_SITE_URL=https://gtm-toolbox.vercel.app
